# Detected Object in camera view

# Object ID/type
int32 object_id
string object_type

# Position in camera frame (normalized 0-1)
float64 x
float64 y

# Size in camera frame (normalized 0-1)
float64 width
float64 height

# Detection confidence (0-1)
float64 confidence
